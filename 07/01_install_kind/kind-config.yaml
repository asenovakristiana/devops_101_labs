kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    # Map host prot 30000 to internal NodePort 30000
    extraPortMappings:
      - containerPort: 30000
        hostPort: 30000
        protocol: TCP
